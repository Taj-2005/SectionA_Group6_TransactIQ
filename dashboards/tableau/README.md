# `dashboards/tableau/`

Tableau deliverables for Executive, Operational, and Safety stakeholders.

- `workbooks/`: `.twb` / `.twbx` files
- `links/`: published dashboard URLs
- `screenshots/`: exported images for README and reports
- `assets/`: fonts, icons, color palettes, supporting files

